name: "Events Collection"
pipelines:
  - slug: "clean_events_registrees"
    transformations:
    
      - clean_registered
      - clean_events
      - merge_dataframes
      - clean_merged
      - create_tags_df
